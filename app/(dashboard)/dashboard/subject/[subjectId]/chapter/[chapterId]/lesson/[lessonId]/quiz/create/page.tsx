'use client';

interface QuizCreatePageProps {
  params: {
    subjectId: string;
    chapterId: string;
    lessonId: string;
  };
}

const QuizCreatePage = ({ params }: QuizCreatePageProps) => {
  return 'div';
};
export default QuizCreatePage;
